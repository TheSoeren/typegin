use crate::keys::define_id;

define_id!(
    /// Stable authored identifier for a dialogue choice, used to identify a
    /// choice across a run (e.g. by a point-and-click UI).
    DialogueOptionId
);
