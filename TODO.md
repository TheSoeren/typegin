# TODO

- [] Interaction effect "take" only allows for taking items that are visible inside the room.
     It must be possible to give the player any item (not all items will be in a room or the inventory).
     hidden_items might be able to fit this already, but if a hidden_item is a scene item, hidden has
     a different effect. A workound is first revealing the item.
     However, this workaround does  not hold up when combining two owned items. That could happen in any
     room.
